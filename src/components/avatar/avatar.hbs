<div class="flex w-100 avatar-div {{className}}">
    <div class="{{iconClass}}">
        {{#if icon }}
            {{icon}}
        {{else}}
            {{> SVGAvatarEmpty }}
        {{/if}}
    </div>
</div>
